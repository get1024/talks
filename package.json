{
  "name": "talks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanup:dist": "rimraf --glob dist",
    "packages:build": "pnpm cleanup:dist && pnpm -r --filter=./packages/* run build",
    "packages:build-base": "pnpm cleanup:dist && pnpm -r --filter=./packages/* run build-base && node scripts/generate-index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "@types/seedrandom": "^3.0.8",
    "seedrandom": "^3.0.5"
  }
}